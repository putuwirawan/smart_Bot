name: katulUjem
on:
  schedule:
    - cron: "*/2 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: katulUjem
        run: |
          curl --request POST \
          --url 'https://smart-bot-ten.vercel.app/api/cronaction' \
          --header 'Authorization: Bearer ${{ secrets.SMARTBOT }}'
